apiVersion: v1
kind: Service

metadata:
  name: saas-backend
  namespace: saas-app

spec:
  type: NodePort

  selector:
    app: saas-backend

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081