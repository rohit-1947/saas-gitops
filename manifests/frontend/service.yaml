apiVersion: v1
kind: Service

metadata:
  name: saas-frontend
  namespace: saas-app

spec:
  selector:
    app: saas-frontend

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort